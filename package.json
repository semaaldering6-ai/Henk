{
  "name": "design-thinking-app",
  "version": "1.0.0",
  "description": "Design Thinking 5-stappen app gebouwd met Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
